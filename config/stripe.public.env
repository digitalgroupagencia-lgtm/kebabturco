# Chaves publicáveis Stripe — ficam no projecto (NÃO colocar em Segredos Lovable).
# pk_* são seguras no browser. Nunca colocar sk_* aqui.
#
# Copie da Stripe → Developers → API keys → modo Test → Publishable key
# Cole abaixo (começa por pk_test_):
VITE_STRIPE_PUBLISHABLE_KEY_TEST=
