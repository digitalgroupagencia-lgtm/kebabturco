OPÇÃO A — Sem créditos Lovable (recomendado, uma vez):

1. Abra https://supabase.com/dashboard/account/tokens e crie um token
2. GitHub → repositório kebabturco → Settings → Secrets and variables → Actions → New secret
   - SUPABASE_ACCESS_TOKEN = o token
   - SUPABASE_PROJECT_ID = kvpssbhclafoymhecmuk
3. Cada push para main publica as funções automaticamente (workflow já no repo)

OPÇÃO B — No Mac, no terminal, na pasta do projecto:
   npx supabase login
   ./scripts/deploy-edge-functions.sh

OPÇÃO C — Chat Lovable (gasta créditos):
Deploy all Supabase edge functions...

Depois: Sync + Publish no botão da Lovable.

Para confirmar que funcionou, abra no browser:
https://kvpssbhclafoymhecmuk.supabase.co/functions/v1/stripe-connect-onboard

Deve aparecer "publicSync": true e "handlerVersion": "2026-06-12-public-sync-v27"

Teste sincronização (não deve dar erro de sessão):
curl -X POST https://kvpssbhclafoymhecmuk.supabase.co/functions/v1/stripe-connect-onboard \
  -H "Content-Type: application/json" \
  -d '{"mode":"sync_status","storeId":"22222222-2222-2222-2222-222222222222"}'
