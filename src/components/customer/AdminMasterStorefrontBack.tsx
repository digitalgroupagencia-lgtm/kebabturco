import AdminMasterQuickNav from "@/components/admin/AdminMasterQuickNav";

/** Atalhos flutuantes no cardápio (admin geral + painel ao vivo). */
export default function AdminMasterStorefrontBack() {
  return <AdminMasterQuickNav mode="overlay" />;
}
