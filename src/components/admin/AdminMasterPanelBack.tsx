import AdminMasterQuickNav from "@/components/admin/AdminMasterQuickNav";

/** Atalho no painel do restaurante, só admin geral vê. */
export default function AdminMasterPanelBack() {
  return <AdminMasterQuickNav mode="inline" />;
}
